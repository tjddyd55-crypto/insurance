/** Injected by Vite `define` from package.json at build time. */
declare const __INSURANCE_WEB_APP_VERSION__: string

/**
 * Injected by Vite `define` at build time (vite.config.ts `WEB_BUILD_ID`).
 * 같은 값이 dist/version.json 에도 방출되어, 실행 중 세션의 새 배포 감지 기준이 된다.
 */
declare const __INSURANCE_WEB_BUILD_ID__: string
