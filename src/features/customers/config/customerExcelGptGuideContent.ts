/**
 * 고객 엑셀 → 샘플 양식 변환용 GPT 지시문 SSOT.
 * CRM 샘플(`customer-upload-sample.xlsx`) 컬럼과 반드시 일치해야 한다.
 *
 * 병력사항은 단일 「병력사항」컬럼(영문키 medical)이며,
 * 과거 안내문의 C·D열 고정 표기는 현재 샘플과 불일치하므로 사용하지 않는다.
 */

export const CUSTOMER_EXCEL_GPT_GUIDE_TITLE = 'GPT를 활용한 고객 엑셀 변환 방법'

export const CUSTOMER_EXCEL_GPT_GUIDE_DESCRIPTION =
  '기존 고객 엑셀과 아래에서 받은 샘플 엑셀을 GPT에 함께 업로드한 뒤, 안내 문구를 복사하여 입력해 주세요.'

export const CUSTOMER_EXCEL_GPT_GUIDE_STEPS = [
  '샘플 엑셀 다운로드',
  '기존 고객 엑셀과 샘플 엑셀을 GPT에 업로드',
  '아래 지시문 복사 후 GPT에 입력',
  '변환된 엑셀을 내려받아 CRM에 업로드',
] as const

export const CUSTOMER_EXCEL_GPT_GUIDE_CAUTIONS_TITLE = '사용 전 확인해 주세요'

export const CUSTOMER_EXCEL_GPT_GUIDE_CAUTIONS = [
  '고객 개인정보가 포함된 파일을 업로드하기 전에 회사 내부 개인정보 처리 기준을 확인해 주세요.',
  '변환된 파일은 반드시 내용을 검토한 뒤 CRM에 업로드해 주세요.',
  'GPT가 자동으로 판단한 매핑이 항상 정확한 것은 아닙니다.',
  '주민번호, 연락처, 계좌번호의 앞자리 0이 유지되는지 확인해 주세요.',
  '샘플 엑셀의 컬럼명과 순서를 변경하면 업로드 오류가 발생할 수 있습니다.',
] as const

/** 클립보드 복사 대상 — 제목·단계·주의사항 제외 */
export const CUSTOMER_EXCEL_GPT_GUIDE_PROMPT = `다음 작업을 수행해줘.

[목표]
기존 고객 엑셀 데이터를, 함께 첨부한 "샘플 엑셀 양식"과 100% 동일한 구조로 변환하여 작성해줘.
CRM 업로드 시 오류가 발생하지 않도록 컬럼명, 컬럼 순서, 셀 형식과 데이터 형식까지 정확히 맞춰줘.

[입력 파일]

1. 기존 고객 데이터 엑셀 파일
2. CRM에서 다운로드한 샘플 엑셀 양식 파일

[작업 지시]

1. 샘플 엑셀의 컬럼명, 컬럼 순서, 시트명과 전체 구조를 절대 변경하지 말고 그대로 유지해줘.
2. 기존 고객 데이터의 각 항목을 샘플 양식의 가장 적절한 컬럼에 정확히 매핑해줘.
3. 기존 파일에 없는 샘플 컬럼은 빈칸으로 유지해줘.
4. 기존 데이터에만 있고 샘플 양식에 적절한 전용 컬럼이 없는 내용은 "메모" 컬럼에 정리해줘.
5. 동일 고객으로 판단되는 중복 행을 임의로 합치거나 삭제하지 말고 원본 행 기준으로 유지해줘.
6. 데이터가 애매한 경우 임의로 새로운 값을 만들지 말고, 가장 유사한 항목에 넣은 뒤 어떤 기준으로 매핑했는지 함께 설명해줘.
7. 변환 완료 후 업로드 가능한 엑셀 파일로 만들어서 제공해줘.

[데이터 형식 규칙 - 매우 중요]

1. 주민등록번호, 휴대폰번호, 일반 전화번호, 계좌번호, 차량번호, 우편번호처럼 앞자리 0이 사라질 수 있는 값은 반드시 텍스트 형식으로 작성해줘.
2. 휴대폰번호와 전화번호는 원본 값이 숫자로 저장되어 있어도 앞자리 0이 유지되도록 처리해줘.
3. 날짜는 샘플 엑셀에서 사용한 날짜 형식을 그대로 따라줘.
4. 숫자, 날짜, 텍스트를 임의로 혼합하지 말고 샘플 셀 형식을 기준으로 맞춰줘.
5. 빈 값에 "없음", "-", "미상", "0" 같은 값을 임의로 넣지 말고 빈칸으로 유지해줘.
6. 수식은 넣지 말고 실제 값으로 작성해줘.
7. 셀 병합, 컬럼 추가, 컬럼 삭제, 컬럼 순서 변경을 하지 말아줘.
8. 숨김 컬럼이나 기존 서식이 있다면 가능한 한 샘플 양식을 그대로 유지해줘.

[병력사항 매핑 규칙]

1. 샘플 엑셀의 「병력사항」컬럼에 수술, 입원, 치료, 진단, 약 복용 등 건강 관련 내용을 넣어줘.
2. 기존 데이터에 병력 관련 컬럼이 여러 개면 내용을 「병력사항」에 알기 쉽게 합쳐줘. (줄바꿈 또는 구분자 사용)
3. 구분할 수 없는 병력 내용은 원문을 유지한 채 「병력사항」에 넣고, 임의로 새로운 병력 내용을 만들어내지 말아줘.

[메모 처리 규칙]

1. 샘플 양식에 별도 컬럼이 없는 기존 데이터는 「메모」컬럼에 넣어줘.
2. 여러 내용을 메모에 넣을 경우 항목명을 함께 표시해줘.
3. 서로 다른 내용을 알아볼 수 있도록 줄바꿈 또는 구분자를 사용해줘.
4. 기존 메모가 있다면 삭제하지 말고 다른 추가 정보와 함께 유지해줘.

[최종 검증]

변환이 끝나면 다음 내용을 확인해줘.

1. 샘플 엑셀과 컬럼명 및 순서가 동일한지
2. 시트명과 구조가 동일한지
3. 필수 텍스트 값의 앞자리 0이 유지되는지
4. 병력사항이 「병력사항」컬럼에 누락 없이 들어갔는지
5. 남은 데이터가 「메모」에 누락 없이 들어갔는지
6. 고객 수가 원본과 동일한지
7. CRM에 바로 업로드할 수 있는 xlsx 파일인지

변환된 엑셀 파일을 제공하고, 마지막에 아래 항목을 간략히 설명해줘.

- 주요 컬럼 매핑 기준
- 애매하여 별도로 판단한 항목
- 메모 컬럼으로 이동한 데이터
- 원본 데이터 중 변환하지 못한 항목`

export const CUSTOMER_EXCEL_GPT_COPY_SUCCESS = 'GPT 지시문을 복사했습니다.'
export const CUSTOMER_EXCEL_GPT_COPY_FAILURE =
  '지시문을 복사하지 못했습니다. 직접 선택하여 복사해 주세요.'
