/** 화면·문서·메타에 쓰는 앱 표시명 (기술 식별자/도메인/API 와 분리). */
export const APP_DISPLAY_NAME = 'ONE FC'

/** `index.html` 기본 `<title>` 및 `document.title` 폴백. */
export const APP_HTML_TITLE = APP_DISPLAY_NAME

/** SEO·공유용 짧은 설명. */
export const APP_META_DESCRIPTION = 'ONE FC — 설계사용 고객관리 서비스.'
